# 자바스크립트 호이스팅(Hoisting)과 클로저(Closure) 개념 정리

## 1. 호이스팅 (Hoisting)

### 정의
호이스팅이란 자바스크립트 엔진이 코드를 실행하기 전, **변수와 함수 선언을 해당 스코프의 최상단으로 끌어올리는 것처럼 동작하는 특성**을 말한다. 실제로 코드가 물리적으로 이동하는 것은 아니며, 엔진이 실행 컨텍스트를 생성하는 과정(컴파일 단계)에서 선언부를 먼저 메모리에 등록하기 때문에 발생하는 현상이다.

### var의 호이스팅
`var`로 선언한 변수는 선언과 동시에 `undefined`로 초기화되어 최상단으로 끌어올려진다. 따라서 선언 이전 코드에서 참조해도 에러 없이 `undefined`가 반환된다.

### let / const의 호이스팅과 TDZ
`let`, `const`도 호이스팅은 되지만, 초기화되지 않은 상태로 끌어올려진다. 선언 전까지 변수에 접근할 수 없는 구간을 **TDZ(Temporal Dead Zone, 일시적 사각지대)**라고 하며, 이 구간에서 접근하면 `ReferenceError`가 발생한다.

### 함수 선언식과 함수 표현식
**함수 선언식**(`function 함수명() {}`)은 선언 자체가 통째로 호이스팅되어 코드상 위치와 무관하게 어디서든 호출할 수 있다. 반면 **함수 표현식**(`var 변수명 = function() {}`)은 변수 선언부만 호이스팅되고 함수 할당은 실행 시점에 이루어지므로, 선언 전에 호출하면 에러가 발생한다.

---

## 2. 클로저 (Closure)

### 정의
클로저란 **함수가 자신이 선언될 당시의 렉시컬 스코프(Lexical Scope)를 기억하여, 함수가 외부 스코프에서 실행되더라도 그 스코프에 접근할 수 있는 함수**를 말한다. 즉, "함수와 그 함수가 선언된 렉시컬 환경의 조합"으로 정의된다.

### 동작 원리
외부 함수가 실행을 마치고 스택에서 제거되더라도, 내부 함수가 외부 함수의 변수를 참조하고 있다면 그 변수는 메모리에서 사라지지 않는다. 이렇게 외부 함수의 변수를 참조하며 살아남는 내부 함수를 클로저라고 부른다.

### 활용 분야
- **데이터 은닉(캡슐화)**: 외부에서 직접 접근할 수 없는 변수를 만들고, 정해진 내부 함수를 통해서만 값을 읽거나 수정하도록 제한할 수 있다.
- **함수 팩토리**: 특정 값을 미리 고정해 둔 새로운 함수를 생성할 때 사용한다.
- **비동기 콜백에서의 상태 유지**: 반복문이나 이벤트 핸들러에서 특정 시점의 변수 값을 기억해야 할 때 활용된다. 이때 `var`(함수 스코프)와 `let`(블록 스코프)의 차이로 인해 클로저가 참조하는 값이 달라질 수 있다.

### 주의점
클로저가 참조하는 변수는 가비지 컬렉션 대상에서 제외되므로, 불필요하게 많이 사용하면 메모리 누수로 이어질 수 있다. 더 이상 필요 없는 클로저 참조는 명시적으로 해제하는 것이 좋다.

---

## 3. 호이스팅과 클로저의 연관성
호이스팅은 "선언이 어떻게 스코프에 등록되는가"에 대한 개념이고, 클로저는 "함수가 자신이 속한 렉시컬 스코프를 어떻게 계속 참조하는가"에 대한 개념이다. 두 개념 모두 자바스크립트의 **스코프(Scope)와 실행 컨텍스트(Execution Context)**를 이해하는 데 핵심이 되는 요소이며, 클로저를 정확히 이해하려면 먼저 호이스팅과 렉시컬 스코프의 동작 방식을 알아야 한다.
